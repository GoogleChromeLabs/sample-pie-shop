<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles/style.css" />
    {{#each scripts}}
      <script src="{{.}}" defer></script>
    {{/each}}
  </head>
  <body>
    {{> header }}
    {{> navbar }}
    <main>
      {{{body}}}
    </main>
    {{> footer }}
  </body>
</html>
